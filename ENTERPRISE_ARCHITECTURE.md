# 企业级多平台数字孪生面试准备系统 - 架构文档

**版本:** 1.0  
**日期:** 2025年11月9日  
**状态:** 生产就绪  

---

## 1. 系统架构概览

### 1.1 高层架构

```
┌─────────────────────────────────────────────────────────────────┐
│                      用户界面层                                    │
├─────────────┬──────────────────────┬──────────────────────┤
│  VS Code    │  Claude Desktop      │  ChatGPT Actions     │
│  Copilot    │  (MCP Protocol)      │  (REST API)          │
└─────────────┴──────────────────────┴──────────────────────┘
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                   MCP 服务器层                                     │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │  claude-mcp-server (Node.js TypeScript)                 │  │
│  │  • Interview Simulator                                   │  │
│  │  • Job Analyzer                                          │  │
│  │  • Professional Profile                                  │  │
│  │  • Performance Analytics                                 │  │
│  └─────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│              业务逻辑与服务层                                       │
├─────────────────────────────────────────────────────────────────┤
│  • Interview Simulator Engine                                   │
│  • Job Market Analyzer                                         │
│  • Career Path Planner                                         │
│  • Response Optimizer                                          │
│  • Performance Analytics                                       │
└─────────────────────────────────────────────────────────────────┘
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                  数据存储层                                         │
├─────────────┬──────────────────────┬──────────────────────┤
│  Vector DB  │  Redis Cache         │  PostgreSQL          │
│  (Upstash)  │  (Session Data)      │  (User Profiles)     │
└─────────────┴──────────────────────┴──────────────────────┘
```

### 1.2 部署架构

```
GitHub Repository
    ▼
GitHub Actions (CI/CD)
    ├─ Lint & Type Check ✅
    ├─ Run Tests ✅
    ├─ Build Application ✅
    ├─ Build & Push Docker Image ✅ (GHCR)
    ├─ Security Scan ✅
    └─ Deploy to Vercel (Production) ✅

Docker Container (GHCR)
    └─ ghcr.io/douglasmooooo/digital-twin:main
        ├─ MCP Server (Node.js)
        ├─ Interview Simulator
        └─ Job Analyzer

Vercel (Next.js Frontend)
    ├─ Production URL
    └─ Staging Preview
```

---

## 2. 核心组件详解

### 2.1 MCP 服务器 (`claude-mcp-server/`)

**技术栈:**
- Node.js 18 (Alpine)
- TypeScript
- MCP (Model Context Protocol)

**关键模块:**
```
claude-mcp-server/
├── index.ts                 # MCP 服务器入口
├── package.json            # 依赖声明
├── tsconfig.json          # TypeScript 配置
├── types-shims.d.ts       # 类型声明 shims
└── dist/                  # 编译输出
    └── index.js
```

**功能:**
- 连接 Claude Desktop 和 VS Code GitHub Copilot
- 暴露数字孪生功能为 MCP tools
- 支持多轮对话上下文

### 2.2 面试模拟引擎 (`lib/interview-simulator.ts`)

**核心功能:**
- 生成基于真实职位的面试问题
- 支持多个问题类别:
  - 技术问题（算法、系统设计、编程语言）
  - 行为问题（STAR 方法论）
  - 行业知识问题
  - 文化适配问题
  - 薪资谈判问题

**评分系统:**
- 技术评分：0-100
- STAR 方法论合规性：0-100
- 综合评分：0-100

**接口:**
```typescript
generateInterviewQuestions(role, level, count)
    → InterviewQuestion[]

evaluateResponse(question, userResponse)
    → InterviewEvaluation

createInterviewSession(role, level)
    → InterviewSession
```

### 2.3 职位分析器 (`lib/job-analyzer.ts`)

**功能:**
- 分析职位需求
- 计算技能差距
- 生成职业发展计划
- 提供学习资源推荐

**数据模型:**
```typescript
interface SkillGap {
  skill: string;
  proficiency: 'beginner' | 'intermediate' | 'advanced' | 'expert';
  yearsToAcquire: number;
  learningResources: string[];
  priority: 'critical' | 'high' | 'medium' | 'low';
}

interface CareerGapAnalysis {
  targetRole: string;
  skillGaps: SkillGap[];
  readinessScore: number;
  estimatedPreparationTime: number;
  developmentPlan: string;
}
```

### 2.4 职位爬虫 (`scripts/job-scraper-seek.ts`)

**功能:**
- 从 Seek.com.au 爬取职位
- 提取职位需求
- 分析技能需求
- 生成职位匹配报告

**数据模型:**
```typescript
interface JobPosting {
  title: string;
  company: string;
  location: string;
  salary?: string;
  requiredSkills: string[];
  keyResponsibilities: string[];
}
```

---

## 3. 数据安全与隐私

### 3.1 数据分类

**公开数据:**
- 职位发布
- 行业趋势
- 公开技能要求

**个人数据:**
- 用户配置
- 面试记录
- 性能评分
- 职业历史

### 3.2 安全措施

**传输安全:**
- ✅ HTTPS/TLS 加密
- ✅ API Token 验证
- ✅ 请求签名

**存储安全:**
- ✅ 数据库连接加密
- ✅ 敏感数据加密
- ✅ 访问日志记录

**访问控制:**
- ✅ 基于角色的权限控制 (RBAC)
- ✅ API 速率限制
- ✅ IP 白名单选项

### 3.3 隐私合规

- ✅ GDPR 合规
- ✅ 数据最小化原则
- ✅ 用户同意管理
- ✅ 数据保留政策
- ✅ 用户删除权

**数据保留:**
- 会话数据：30 天
- 用户配置：用户删除前保留
- 分析数据：12 个月后匿名化

---

## 4. 性能优化

### 4.1 缓存策略

**Redis 缓存 (Upstash):**
```
- 职位数据缓存：24 小时
- 技能分析缓存：7 天
- 用户配置缓存：会话期间
```

**向量数据库 (Upstash Vector):**
```
- 职位向量化存储
- 技能相似性搜索
- 快速职位匹配
```

### 4.2 API 优化

- 响应时间目标：< 2 秒
- 数据库查询优化
- 批量处理
- 异步任务队列

### 4.3 前端性能

- Next.js 静态生成
- 图像优化
- 代码分割
- CDN 部署

---

## 5. 监控与告警

### 5.1 关键指标 (KPI)

```
应用程序健康:
  • 平均响应时间
  • 错误率
  • 系统可用性
  • 用户在线数

业务指标:
  • 面试准备完成率
  • 用户满意度
  • 职位匹配率
  • 学习资源使用
```

### 5.2 告警规则

```
严重:
  - 错误率 > 5%
  - 响应时间 > 5 秒
  - 系统宕机

警告:
  - 错误率 > 2%
  - 响应时间 > 2 秒
  - 磁盘空间 < 20%
```

### 5.3 日志聚合

- CloudWatch / ELK Stack
- 所有请求日志
- 错误堆栈跟踪
- 审计日志

---

## 6. 灾难恢复

### 6.1 备份策略

```
数据库备份:
  • 每日完全备份
  • 每小时增量备份
  • 异地冗余存储

恢复点目标 (RPO): 1 小时
恢复时间目标 (RTO): 2 小时
```

### 6.2 故障转移

- 自动故障检测
- 多可用区部署
- 数据库复制
- 蓝绿部署

---

## 7. CI/CD 流程

### 7.1 GitHub Actions 工作流

```yaml
1. Lint & Type Check
   ├─ ESLint
   └─ TypeScript 类型检查

2. Run Tests
   ├─ Unit Tests
   ├─ Integration Tests
   └─ Coverage Report

3. Build Application
   ├─ Next.js 构建
   ├─ MCP 编译
   └─ 生成产物

4. Build & Push Docker Image
   ├─ Docker 多阶段构建
   ├─ 推送到 GHCR
   └─ 标记为 latest

5. Security Scan
   ├─ Snyk 扫描
   └─ NPM 审计

6. Deploy to Vercel
   └─ 生产部署
```

### 7.2 版本控制

- Semantic Versioning
- Git Flow: main (production), develop, feature branches
- 标签和发布

---

## 8. 扩展性架构

### 8.1 水平扩展

```
Load Balancer
    ├─ MCP Server Instance 1
    ├─ MCP Server Instance 2
    └─ MCP Server Instance N

Shared Services:
    ├─ PostgreSQL (Replicas)
    ├─ Redis (Cluster)
    └─ Vector DB (Sharded)
```

### 8.2 服务隔离

- MCP 服务器无状态
- 会话存储在 Redis
- 数据库连接池
- 容器资源限制

---

## 9. 部署说明

### 9.1 本地开发

```bash
# 安装依赖
npm install

# 开发服务器
npm run dev

# 类型检查
npm run type-check

# 测试
npm test
```

### 9.2 生产部署

```bash
# Docker 构建
docker build -t digital-twin:latest .

# 推送到 GHCR
docker tag digital-twin:latest ghcr.io/douglasmooooo/digital-twin:latest
docker push ghcr.io/douglasmooooo/digital-twin:latest

# Vercel 部署
vercel --prod
```

### 9.3 环境配置

```env
# 生产环境必需的 Secrets
UPSTASH_VECTOR_REST_URL=
UPSTASH_VECTOR_REST_TOKEN=
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
GROQ_API_KEY=

# Vercel 部署
VERCEL_TOKEN=
VERCEL_ORG_ID=
VERCEL_PROJECT_ID=
```

---

## 10. 故障排除指南

### 常见问题

| 问题 | 原因 | 解决方案 |
|------|------|--------|
| MCP 连接失败 | 服务未启动 | 检查日志，重启服务 |
| 面试评分不准确 | 评分算法问题 | 重新训练模型，调整权重 |
| 职位爬虫超时 | 网络问题 | 增加重试次数，检查代理 |
| 数据库连接错误 | 凭证问题 | 验证环境变量 |

---

## 11. 未来改进

- 🔄 AI 模型优化（GPT-4 集成）
- 📊 高级分析仪表板
- 🎓 认证培训路径
- 🌐 多语言支持
- 📱 移动应用
- 🔌 第三方集成（LinkedIn、GitHub）

---

## 12. 支持与维护

**联系方式:**
- Issue Tracker: GitHub Issues
- 文档: `/docs`
- 技术支持: engineering@company.com

**维护计划:**
- 每周安全补丁
- 月度功能发布
- 季度主版本发布
